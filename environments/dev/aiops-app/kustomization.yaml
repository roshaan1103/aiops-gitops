apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../apps/aiops-app

patches:
  - target:
      kind: Deployment
      name: aiops-app
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
