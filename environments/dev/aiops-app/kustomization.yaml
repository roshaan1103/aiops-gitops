apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Base app
  - ../../../apps/aiops-app

  # AI Anomaly Service (NEW service)
  - ai-anomaly-deployment.yaml
  - ai-anomaly-service.yaml
  - ai-anomaly-servicemonitor.yaml
  - ai-anomaly-alert.yaml
  - ai-alertmanager-config.yaml

  # Observability
  - grafana-dashboard-config.yaml
  - grafana-dashboard-provider.yaml
  - grafana-datasource.yaml

  # Ingress
  - grafana-ingress.yaml
  - grafana-tls.yaml
  - argocd-ingress.yaml
  - argocd-tls.yaml

patches:
  - target:
      kind: Deployment
      name: aiops-app
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
