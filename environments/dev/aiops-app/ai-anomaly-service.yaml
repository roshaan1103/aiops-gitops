apiVersion: v1
kind: Service
metadata:
  name: ai-anomaly-service
  namespace: monitoring
  labels:
    app: ai-anomaly-service
spec:
  selector:
    app: ai-anomaly-service
  ports:
  - name: http
    port: 8000
    targetPort: 8000
