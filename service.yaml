apiVersion: v1
kind: Service
metadata:
  labels:
    app: aiops-app-monitor
  name: aiops-service
spec:
  type: NodePort
  selector:
    app: aiops-app
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007
